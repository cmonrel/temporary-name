export const Attribute_Checks = () => {
  return (
    <section>
      <br />
      <p>
          Attribute checks are rolled only with the dice from the corresponding attribute.
          A skill check basically becomes an attribute check if the skill level is 0.
      </p>
    </section>
  )
}
